import { ecommerceModel } from '../../models/ecommerceModel';

export const MOCK_ECOMMERCES: ecommerceModel[] = [
  { id: 1 },
  { id: 2 },
];

export const mockecommerce = MOCK_ECOMMERCES[0]!;
export const mockecommerces = MOCK_ECOMMERCES;
