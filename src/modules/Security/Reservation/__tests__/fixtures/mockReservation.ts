import { ReservationModel } from '../../models/ReservationModel';

export const MOCK_RESERVATIONS: ReservationModel[] = [
  { id: 1 },
  { id: 2 },
];

export const mockReservation = MOCK_RESERVATIONS[0]!;
export const mockReservations = MOCK_RESERVATIONS;
