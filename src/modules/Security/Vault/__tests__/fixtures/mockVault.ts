import { VaultModel } from '../../models/VaultModel';

export const MOCK_VAULTS: VaultModel[] = [
  { id: 1 },
  { id: 2 },
];

export const mockVault = MOCK_VAULTS[0]!;
export const mockVaults = MOCK_VAULTS;
